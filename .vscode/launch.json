{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "▶ Run相当: ターミナルで実行（デバッグなし）",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}",
      "justMyCode": true,
      "noDebug": true
    },
    {
      "name": "Python: 現在のファイル（デバッグ）",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}",
      "justMyCode": true
    }
  ]
}
